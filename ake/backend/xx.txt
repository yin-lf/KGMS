conda create -n KEP python=3.12
conda activate KEP
pip install flask flask-cors neo4j marshmallow requests
pip install pytest
cd E:\ke2025\project1\ake\backend

启动neo4j
在akb/const.py中修改neo4j账号密码
python app.py # 启动服务
pytest akb/tests/ # 验证后端类、方法的逻辑是否正确
pytest test/ # 模拟了前端可能发出的请求，测试后端接口是否正确响应